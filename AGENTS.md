# AGENTS.md

本文件为在本仓库中工作的 AI agent 提供上下文与规则。

## 这个项目是什么

本仓库是一本**中文源码解析书**的工程：《Pi Agent 源码解析》。它从源码角度剖析 Pi Agent（一个刻意保持最小化的开源编码 Agent）的实现。书的全部内容是自包含的 Markdown 文档，位于 `book/`。根目录 `README.md` 是全书的入口（简介 + 目录）。

本书的写作参考了 [Claude Code 源码解析](https://github.com/kuwii/claude-code-from-source-cn)（一本 Claude Code 源码解析书）的叙事结构，但分析对象与章节结构均针对 Pi Agent 重新设计。全书主线是 Pi Agent 的**最小化核心哲学**（"复杂性守恒"：核心保持最小，把复杂性推到扩展/容器/可选包等边缘）。

## 仓库结构

```
.
├── README.md          # 全书入口：简介 + 目录（链接指向 book/）
├── AGENTS.md          # 本文件
├── book/              # 书的正文：ch01–ch17，自包含 Markdown
│   ├── ch01-architecture.md
│   ├── ...
│   └── ch17-epilogue.md
├── pi-agent-src/      # git 子模块 → https://github.com/earendil-works/pi.git
│                      #   被分析的 Pi Agent 源码。只读参考。
│                      #   注意：它有自己的 AGENTS.md，那是给"开发 pi"用的，不适用于本仓库。
├── .gitmodules
└── LICENSE
```

## 源码权威与核对原则（最重要）

- **`pi-agent-src/` 是唯一源码权威。** 书中所有文件路径均**相对于 `pi-agent-src/`**（例如 `packages/agent/src/agent-loop.ts` 指 `pi-agent-src/packages/agent/src/agent-loop.ts`）。
- **在断言任何代码事实之前，先读真实源码核对。** 函数名、类型签名、字段名、行为、行号——一律以 `pi-agent-src/` 当前内容为准。**不要**凭记忆或凭书中已有文字推断；书的内容可能已过时或不精确。
- 分析基于的版本为 monorepo 各包 `v0.83.0`。若子模块指向其他版本，以子模块实际内容为准，并注意书中表述可能需要同步更新。
- 引用代码时，函数名/类型名/字段名/文件位置必须可检索核对；为讲解可省略部分类型标注或分支，但不得虚构不存在的标识符。

## 书的写作规范（编辑或新增章节时保持一致）

- **语言**：中文行文；代码、标识符、文件路径、CLI 命令、JSON 键保持英文，不翻译。
- **章节文件命名**：`book/chNN-slug.md`（NN 为两位序号）。
- **每章的典型结构**：开场钩子小节 → 正文（含 mermaid 图、表格、真实代码摘录）→ 必要的实例演练 → **"实践应用"**（提炼可迁移模式）→ 通常以 **"总结"** 收尾。
- **图表**：只用**静态 mermaid** 图（GitHub 原生渲染），不使用动画。
- **交叉引用**：用"第 N 章"形式；新增/改动章节时检查相关引用是否仍然准确。
- **主线一致**：保持"最小化核心 / 复杂性守恒 / 与 Claude Code 综合体路线对照"这条主线连贯。
- 修改既有章节时，模仿其既有风格与结构，不要引入不一致的语气或格式。

## Git 规范

- **commit message 用英文。**
- **只暂存明确的路径**（`git add <path1> <path2>`）；**绝不**使用 `git add -A` / `git add .`。
- 书的内容在外层仓库提交。`pi-agent-src` 是子模块——**不要**进入其中提交，也不要改动其指针，除非用户明确要求。
- 提交前用 `git status` 确认只包含本次改动的文件。

## 禁止事项

- **不要修改 `pi-agent-src/` 的任何源码**——它是被分析的对象，是只读参考。
- **不要把 `pi-agent-src/AGENTS.md` 里的规则当作本仓库的规则**——那些规则用于开发 pi 本身（如其 biome 检查、测试命令、changelog 流程），与"写这本书"无关。
- 不要在未读源码核对的情况下，凭印象书写或"修正"代码引用。
